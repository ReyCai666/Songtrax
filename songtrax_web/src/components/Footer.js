import React from "react";

/**
 * Footer Component
 * The footer component that is displayed on every page.
 * 
 * @component
 * @returns {JSX.Element} The Rendered footer component.
 */
export default function Footer() {
    return (
        <footer className="page-footer"></footer>
    );
};